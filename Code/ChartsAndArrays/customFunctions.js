/*
 * Example of custom functions for chart computation
 */

/*
 * Custom function f(x)
 */
function efofex (x) {
	  return Math.cos(x)*x;
}

/*
 * Custom function g(x)
 */function geeofex (x) {
	    return Math.sin(x)*Math.sin(x);
	  }
